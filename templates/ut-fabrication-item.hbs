{{!-- What the book says about an tool, material, fabricator or gravity device
     (Ultra-Tech pp. 76-94). --}}
<div class="isub" data-gcc-ut-fabrication-section>
  <div class="isub-head">{{localize "GCC.UT.Fab.Title"}}</div>
  {{#each lines as |line|}}<p class="ihint">{{line}}</p>{{/each}}
</div>
