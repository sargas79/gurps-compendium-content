{{!-- A vehicle's components, protection and crew conditions (High-Tech pp. 228-229, 234-235). --}}
<div class="isub" data-gcc-ht-vehicles-section>
  <div class="isub-head">{{localize "GCC.HT.Vehicles.Title"}}</div>
  {{#each lines as |line|}}<p class="ihint">{{line}}</p>{{/each}}
</div>
