{{!-- A security device's figures by TL (Ultra-Tech pp. 101-111). --}}
<div class="isub" data-gcc-ut-security-section>
  <div class="isub-head">{{localize "GCC.UT.Security.Title"}}</div>
  {{#each lines as |line|}}<p class="ihint">{{line}}</p>{{/each}}
</div>
