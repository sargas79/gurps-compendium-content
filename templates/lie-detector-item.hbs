{{!-- A lie detector: how its reading is taken and what it does to
     Interrogation, and the reading it holds (High-Tech pp. 215-216), in the
     words of the book whose table applies. --}}
<div class="isub" data-gcc-lie-detector-section>
  <div class="isub-head">{{title}}</div>
  {{#each lines as |line|}}<p class="ihint">{{line}}</p>{{/each}}
</div>
