{{!-- What the book says about an interface, VR rig or media program
     (Ultra-Tech pp. 47-59). --}}
<div class="isub" data-gcc-ut-interface-section>
  <div class="isub-head">{{localize "GCC.UT.Interface.Title"}}</div>
  {{#each lines as |line|}}<p class="ihint">{{line}}</p>{{/each}}
</div>
