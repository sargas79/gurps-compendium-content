{{!-- The Training Sequence (Martial Arts p. 147). --}}
<div class="gc-head"><span class="gc-label">{{title}}</span></div>
{{#each lines as |line|}}
<div class="gc-mod"><strong>{{line.name}}</strong>: {{line.text}}</div>
{{/each}}
