{{!-- Encryption gear and code-breaking programs (High-Tech p. 211). --}}
<div class="isub" data-gcc-ht-codes-section>
  <div class="isub-head">{{localize "GCC.HT.Codes.Gear.Title"}}</div>
  {{#each lines as |line|}}<p class="ihint">{{line}}</p>{{/each}}
</div>
